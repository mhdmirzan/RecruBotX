# Production environment variables (used by Vercel build)
REACT_APP_NAME=RecruBotX
REACT_APP_ENV=production
REACT_APP_DEFAULT_LANGUAGE=en
REACT_APP_VERSION=1.0.0

# Use relative path so requests go through Vercel's rewrite proxy
# (vercel.json rewrites /api/* -> https://recrubotx-1-dg0g.onrender.com/api/*)
REACT_APP_API_URL=/api
